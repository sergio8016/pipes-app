import { ToggleCasePipe } from './toggle-case.pipe';

describe('ToggleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
